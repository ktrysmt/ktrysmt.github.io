---
layout: post
title: "2025年お世話になったツールたち"
date: 2025-12-31 22:00:00 +0900
categories: LLM
published: true
use_toc: false
---

年末らしいものを一応。

## Neovim関連

### claudecode.nvim


- `<leader>cc` でClaude Codeターミナル起動
- `<leader>ca` でバッファパスをClaude Codeに送信
- diff受け入れ/拒否のキーマップ設定

これのおかげで引き続きvimに引きこもれてます。

### gemini.nvim/Copilot.lua


- `<Shift+Tab>` での補完受け入れ

惰性で使ってるけど補完に頼ることが最近あまりないかも。
書かせるならしっかりプロンプト書くので。


### neo-tree.nvim

ファイラ乗り換えはちょっと手間かかりましたが、やってよかった

- [自然ソートとtrashコマンドの実装](https://ktrysmt.github.io/blog/neo-tree-tips/)
  - 数字を含むファイル名の自然順ソート
  - d キーで trash コマンドへのマッピング

### lexima.vim

コマンドエイリアス最適化とか

- :s → :s/\v (very magic)
- :gd → :DiffviewOpen
- :gbr でGitHub Browse連携
  ```lua
  local my_browser = vim.env.MY_BROWSER
  if my_browser and my_browser ~= "" then
    local escaped = vim.fn.shellescape(my_browser)
    vim.cmd(string.format([[LeximaAlterCommand gbr !%s<space>$(gh<space>browse<space>-n<space>%%)]], escaped))
  else
    vim.cmd [[LeximaAlterCommand gbr !gh<space>browse<space>%]]
  end
  ```
- :tig → :Vsr|TigOpenCurrentFile
  ```vim
  function! g:LeximaVsplitRight(...) abort
    let save_splitright = &splitright
    set splitright
    execute 'vsplit' (a:0 > 0 ? a:1 : '')
    let &splitright = save_splitright
  endfunction
  command! -nargs=? -complete=file Vsr call g:LeximaVsplitRight(<f-args>)
  ```


### outline.nvim

ft=yaml.ansible なファイルをよく扱うので、outline.nvimにてctagsプロバイダを利用。
一部バグあったのでfixしたり。

- [outline-ctags-provider.nvimのfix](https://github.com/epheien/outline-ctags-provider.nvim/pull/2)


### mini.align

これいれてよかった。

- `<Leader>l` でテキスト整列

### gitsigns.nvim

hunk ナビゲーション強化など。

- gw/gb で次/前のhunkへ移動
- g+/g- でstage/unstage

```lua
local opt = { silent = true }
vim.keymap.set("n", "gw", function()
  gitsigns.nav_hunk('next', { target = 'all' })
end, opt)
vim.keymap.set("n", "gb", function()
  gitsigns.nav_hunk('prev', { target = 'all' })
end, opt)
vim.keymap.set({ "n", "v" }, "g+", function()
  gitsigns.stage_hunk()
end, opt)
vim.keymap.set({ "n", "v" }, "g-", function()
  gitsigns.undo_stage_hunk()
end, opt)
```

### fzf-vim

[ファイラを変えたので Ripgrep で neo-tree対応させたり。](https://github.com/ktrysmt/dotfiles/blob/d9778a39e5edae36ebcc99b7607ce283c19beaed/nvim/lua/plugins/fzf-vim.lua)
地味に細かい修正を入れていた。最終的には期待する動作をしてくれてなにより。

- neo-tree/oilバッファからの自動ウィンドウ切り替え
- ctrl-v でvsplit時の条件分岐


## ほか

- [zmvもいれたりdeferを最適化したり](https://github.com/ktrysmt/dotfiles/blob/e88a6d3c5657a8feccc3b7d3ada398c0e241a144/zsh/sheldon.plugins.toml)
- exaからezaに乗り換えとか

### Karabiner-Elements

LLM向け最適化をいくつか

- terminal にて `<C-w>` で強制eisuなど、vim向け修正。
- どのアプリでも `<Shift-Enter>` で改行するように統一させたり。

### tmux

- 表示する色の数が増えそうだったのでいい加減 [True Color (tmux-256color) に対応](https://ktrysmt.github.io/blog/true-color-with-tmux/)

### WSL

wslを使うことが増えた

- クリップボード関連：win32yank.exe など
- git-bash で git-credential-manager対応したり


## 感想

LLMを除けば総じて微調整が多く盆栽の感じがあるが、地味な改善をしたりタイプ数減らそうとしたり、細かいストレスのケアは継続しているようです。
2025年もストレス駆動開発でした。


